asp.net core 8.0
angular v 17
node js version 24, angular 17 does not provide node js 24 ?
client=> ANGOLAR
serve => asp.net
how to install CSaneks on remote server:
by FTP FILE MOZILA:не е необходимовсички ф-ве.
може би ако поствим Csnakes и в server app, и при publish ще се зареди на сървара.
иначе от командата fromredistributer не може.
host: win1042.site4now.net
usernme:hradoevdar-001
pasw:Hradoev67Asp!
тук паролата беше друга, но я смених
за да работи на сървара с твоя core 8.0, web.config => inprocess
може да се разгледа и properties/launchSettings.json
ако на сървара няма фолдер python, комндата withhome, ще хвърли изключение.

DataTable:
в index.html трябва да присъства:
<script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.js"></script>

в testpythonaspng.client/src има файл:
proxy.conf.js
const { env } = require('process');

const target = env.ASPNETCORE_HTTPS_PORT ? `https://localhost:${env.ASPNETCORE_HTTPS_PORT}` :
  env.ASPNETCORE_URLS ? env.ASPNETCORE_URLS.split(';')[0] : 'https://localhost:7298'; => слуша servera

const PROXY_CONFIG = [
  {
    context: [
      "/weatherforecast",
      "/api/analysis" => това са api/controller end points
    ],
    target,
    secure: false
  }
]

module.exports = PROXY_CONFIG;

in develop mode
usestaticfiles => clientapp
python write file on clientapp
controller return localhost:with port from property/lainchsetting => the port of server asp.net
angular img src = localhost:asp.net/clientapp

in publish we should use clean url => wwwroot
python should write on wwwroot

